# dependencies
node_modules/
mini-services/*/node_modules/

# copied from node_modules/@embedpdf/pdfium by the postinstall script —
# self-hosted so the PDF preview never depends on jsDelivr at runtime,
# but the binary itself is a build artifact, not source.
/public/pdfium.wasm

# next.js
.next/
out/
next-env.d.ts

# typescript
*.tsbuildinfo

# env
.env
.env*.local

# database
*.db
*.db-journal
prisma/*.db

# local file storage (simulated S3 buckets) — root-anchored so this
# doesn't also match src/app/api/storage/, which is real source code
/storage/

# logs
dev.log
server.log
npm-debug.log*

# playwright
/test-results/
/playwright-report/
/blob-report/

# misc
.DS_Store
*.pem
